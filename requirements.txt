# tiny-cl dependencies
# All experiments are single-file Kaggle scripts that auto-install missing deps,
# but this file pins the full set for reproducibility.

# Core
torch>=2.0.0
transformers>=5.0.0          # required for native LFM2.5-350M support
peft>=0.10.0                 # LoRA adapters
accelerate>=0.20.0           # device_map, bf16 dispatch
datasets>=2.12.0

# Data / IO
numpy
gdown                        # TRACE benchmark download from Google Drive

# Plotting (V2 framework + dashboard)
matplotlib
tqdm

# Optional: Modal.com runner (V2 only)
# modal
